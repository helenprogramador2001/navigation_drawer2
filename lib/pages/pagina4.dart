import 'package:flutter/material.dart';

class Pagina4 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Center(
      child: Text('Pantalla Pagina4',
        style: TextStyle(
          fontSize: 50,
          fontWeight: FontWeight.bold ,
          color: Colors.amber,
        ),
      ),

    );
  }

}